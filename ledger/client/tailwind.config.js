/** @type {import('tailwindcss').Config} */
module.exports = {
 mode: 'jit', // Enable Just-In-Time (JIT) mode
  content: [
    './src/**/*.{html,js,jsx,ts,tsx}', // Include all template files
  ],
  theme: {
    extend: {
    
    },
  },
  plugins: [],
}

